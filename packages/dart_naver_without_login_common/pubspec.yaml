name: dart_naver_without_login_common
description: Shared authentication and HTTP utilities for unofficial NAVER API packages.
version: 0.1.0
repository: https://github.com/Coaspe/dart_naver_without_login

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  http: ^1.6.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
